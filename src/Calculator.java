public interface Calculator {
    double getSum(double first, double second);
    double getSub(double first, double second);
    double getMul(double first, double second);
    double getDiv(double first, double second);
}
